export interface Ship {
    id: number;
    name: string;
    length: number;
    width: number;
    code: string;
}

export interface ShipData {
    name: string;
    length: number;
    width: number;
    code: string;
  }